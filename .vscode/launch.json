{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "zig build run",
      "program": "zig",
      "args": ["build", "run"],
      "cwd": "${workspaceFolder}",
      "env": {
        "FORCE_COLOR": "1"
      },
      "console": "internalConsole"
    }
  ]
}
